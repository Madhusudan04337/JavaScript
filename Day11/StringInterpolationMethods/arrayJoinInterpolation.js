// An array of strings can be joined into a single string. This method is more verbose.

const name = "Alice";
const parts = ["Hello, ", name, "!"];
const result = parts.join("");
console.log(result);