// Template literals (ES6+) are the preferred way to interpolate strings.
// They use backticks (`) and placeholders of the form ${expression}.

const name = "Alice";
const result = `Hello, ${name}!`;
console.log(result);