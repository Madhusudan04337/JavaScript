// The Concatenate Assignment Operator (+=) appends one string to another.
// It modifies the original string in place.

let s1 = "Code";
let s2 = "Hub";

s1 += s2; // This is equivalent to s1 = s1 + s2;
console.log(s1);