// The replaceAll() method returns a new string after replacing all matches
// of a pattern with a specified replacement.

let str = "Mind, Power, Power, Soul";

// Replaces all occurrences of "Power" with "Space"
let part = str.replaceAll("Power", "Space");

console.log(part);

/*
Expected Output:
Mind, Space, Space, Soul
*/