// The split() method splits a string into an array of substrings.
// It takes a separator as an argument.

let topic = 'Intro ';
let title = 'learn-to-code-well';

// Split string on '-'.
console.log(title.split('-'));

/*
Expected Output:
[ 'learn', 'to', 'code', 'well' ]
*/