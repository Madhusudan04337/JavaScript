// The reverse() method reverses an array in place. The first array element
// becomes the last, and the last array element becomes the first.

let a = [1, 2, 3, 4, 5];
a.reverse(); // Modifies the original array.

console.log(a);

/*
Expected Output:
[ 5, 4, 3, 2, 1 ]
*/