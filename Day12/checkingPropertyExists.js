// You can check if an object has a specific property using the 'in' operator
// or the .hasOwnProperty() method.

let car = { model: "Tesla" };

// Using the 'in' operator
console.log("color" in car);

// Using .hasOwnProperty()
console.log(car.hasOwnProperty("model"));